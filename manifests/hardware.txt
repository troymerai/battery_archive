# hardware.txt — LOCK.md 의 interval 항목이 참조하는 하드웨어 기록
#
# interval 항목(학습 지표 등)은 난수 초기화와 GPU 커널 비결정성 때문에
# 바이트 단위로 같을 수 없습니다. 어떤 기계에서 잰 구간인지 남기지 않으면
# 그 구간이 무엇을 뜻하는지 알 수 없습니다.
#
# 학습을 처음 돌린 사람이 채웁니다. 라벨 검증만 하는 첫 태그 범위에서는
# 비워 두어도 됩니다.
#
# 채우는 법 (Windows PowerShell):
#   nvidia-smi
#   python -c "import torch; print(torch.__version__, torch.version.cuda, torch.backends.cudnn.version())"
#   systeminfo | Select-String "OS 이름","OS 버전"

[machine]
label            = (미정)          # 예: 117호-A
os               = (미정)
cpu              = (미정)
ram_gb           = (미정)

[gpu]
model            = (미정)          # 예: NVIDIA RTX 4090
count            = (미정)
vram_gb          = (미정)
driver           = (미정)
cuda_runtime     = (미정)
cudnn            = (미정)

[torch]
version          = (미정)
cuda_build       = (미정)

[determinism]
seed             = (미정)
cudnn_benchmark  = (미정)
cudnn_determin   = (미정)
tf32_allowed     = (미정)

[note]
# 위 항목 중 하나라도 바뀌면 interval 기준값을 다시 재야 합니다.
# 바꾼 사람이 LOCK.md 의 해당 행과 이 파일을 같이 갱신하십시오.
