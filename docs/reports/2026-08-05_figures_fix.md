# 그림의 Zn-ion 묶음 정정 — 완료 보고

2026-08-05 · CC · 앞선 보고 `2026-08-05_figures.md` 의 §7-1 정정

재학습·재파싱하지 않았습니다. 이미 파싱된
`experiments/results/curves/*.json` 의 `group` 필드만 맞바꾸고 그림을 다시
그렸습니다. **숫자는 한 개도 바뀌지 않았습니다** (§1-3 에서 대조).

| 만든 것 · 고친 것 | |
|---|---|
| `train/regroup_znion.py` | 묶음 재지정 (1회성, 멱등) |
| `train/curves.py` | `SOURCES` 의 Zn-ion 묶음 지정 정정 — **실행하지 않았습니다** |
| `train/figures.py` | 그림 2 를 파일명이 아니라 `group` 으로 고르게 변경 · 그림 4 부제 추가 |
| `figures/fig2_domains.png` · `fig5_early_stop.png` | 재지정 반영 |
| `figures/fig4_liion_vs_calb.png` | 부제 한 줄 추가 (지시 §5) |
| `figures/CONDITIONS.md` | §0 정본 기준 신설 · §2 정정 |

---

## 1. 묶음을 바꾼 실행 9건

`main` 과 `diagnostic` 을 Zn-ion 9회에 한해 맞바꿨습니다. 세 모델 전부
포함합니다.

| 실행 | 새 `main` (정본) | 새 `diagnostic` |
|---|---|---|
| `MLP_Zn-ion_s2021` | lr 5e-5 · `zn_lr9.log` | lr 5e-5 · `20260804-154322_*` |
| `MLP_Zn-ion_s42` | lr 5e-5 · `zn_lr9.log` | lr 5e-5 · `20260804-154322_*` |
| `MLP_Zn-ion_s2024` | lr 5e-5 · `zn_lr9.log` | lr 5e-5 · `20260804-154322_*` |
| `CPMLP_Zn-ion_s2021` | lr 5e-5 · `zn_lr9.log` | **lr 5e-4** · `20260804-154322_*` |
| `CPMLP_Zn-ion_s42` | lr 5e-5 · `zn_lr9.log` | **lr 5e-4** · `20260804-154322_*` |
| `CPMLP_Zn-ion_s2024` | lr 5e-5 · `zn_lr9.log` | **lr 5e-4** · `20260804-154322_*` |
| `CPTransformer_Zn-ion_s2021` | lr 5e-5 · `zn_lr9.log` | **lr 1e-3** · `20260804-154322_*` |
| `CPTransformer_Zn-ion_s42` | lr 5e-5 · `zn_lr9.log` | **lr 1e-3** · `20260804-154322_*` |
| `CPTransformer_Zn-ion_s2024` | lr 5e-5 · `zn_lr9.log` | **lr 1e-3** · `20260804-154322_*` |

재지정 후 **`main` 36회 · Zn-ion 학습률 집합 `{5e-5}`** — 36회 전부 5e-5 로
통일됩니다.

### 1-1. MLP 두 판은 값이 완전히 같았습니다

지시서가 예고한 대로입니다. 최종 MAPE 세 값(1.1319 · 1.4195 · 1.0945)만이
아니라 **전 에폭 지표 · 최종 보고값 · 인자 사전이 모두 일치**합니다.

| seed | 에폭 수 | 전 에폭 지표 | 최종 보고값 | 인자 사전 | Test MAPE |
|---|---:|:--:|:--:|:--:|---:|
| 2021 | 14 / 14 | 동일 | 동일 | 동일 | 1.1319 |
| 42 | 10 / 10 | 동일 | 동일 | 동일 | 1.4195 |
| 2024 | 10 / 10 | 동일 | 동일 | 동일 | 1.0945 |

값이 같으므로 **출처 일관성을 위해 `zn_lr9.log` 쪽으로 통일**했습니다.
멈출 사유가 없었습니다.

### 1-2. 정본이 맞다는 방증

새 `main` 의 `Zn-ion CPMLP` 3 seed Test MAPE 는 0.545 · 0.6375 · 0.7047 이고
평균 ± 표본표준편차가 **0.6291 ± 0.0802** 입니다. 보고서 부록 B ·
Table 3 대조표의 **0.629 ± 0.080** 과 소수 셋째 자리까지 맞습니다.
재지정 전 `main`(5e-4)은 1.3542 · 0.9473 · 0.8938 이라 표와 맞지 않았습니다.

### 1-3. 숫자를 건드리지 않았음을 대조했습니다

재지정 직전 58개 JSON에 대해 `group` · `tag` 를 뺀 나머지 전체의 SHA-256 을
떠 두고 재지정 후 다시 떴습니다. **58/58 동일** — 달라진 파일 0건입니다.

### 1-4. 파일명은 바꾸지 않았습니다 — `group` 이 근거입니다

지시가 "묶음 표시만" 이었으므로 파일명을 그대로 뒀습니다. 그 결과
**Zn-ion 만 접미사 규칙이 뒤집힙니다.**

| | 다른 세 도메인 | Zn-ion |
|---|---|---|
| 접미사 없는 파일 | `main` | **`diagnostic`** (5e-4) |
| `__lr5e-05` | — | **`main`** (정본) |

`train/figures.py` 의 그림 2 를 파일명이 아니라 `group == "main"` 으로
고르도록 바꿔 이 함정을 막았습니다(`main_run()`). `CONDITIONS.md` §0 에도
적었습니다. **파일명을 맞추려면 이름을 바꿔야 하는데 이번 지시 범위 밖이라
하지 않았습니다** — §6 에 올립니다.

### 1-5. `train/curves.py` 도 고쳤으나 실행하지 않았습니다

`SOURCES` 에서 `20260804-154322_*` → `diagnostic`, `zn_lr9.log` → `main`
으로 바꿨습니다. 다음에 누가 파서를 다시 돌려도 같은 묶음이 나오게 하려는
것이고, **지시대로 재파싱은 하지 않았습니다.** 지금 디스크 상태와
`SOURCES` 가 만들어 낼 상태가 일치합니다(파일명 · 태그 · 묶음 전부).

---

## 2. `CPTransformer` Zn-ion 의 5e-5 판 — 존재합니다

`zn_lr9.log` 에 9회가 전부 들어 있습니다. `CONDITIONS.md` 에 적혀 있던
`1e-3` 는 **문서 지정값 판(`20260804-154322_*`)의 값**이었고, 5e-5 판은
따로 있습니다. 건너뛴 모델은 없습니다.

| 실행 | 5e-5 판 종료/최적 에폭 | 5e-5 판 Test MAPE | 1e-3 판 Test MAPE |
|---|---|---:|---:|
| `CPTransformer_Zn-ion_s2021` | 26 / 21 | 0.5554 | 0.6803 |
| `CPTransformer_Zn-ion_s42` | 12 / 7 | 0.5395 | 0.7516 |
| `CPTransformer_Zn-ion_s2024` | 34 / 29 | 0.4319 | 0.4954 |

---

## 3. 재생성 후 그림별 변경 — 2 · 5 만 바뀌었습니다

두 단계로 나눠 검증했습니다. 판정은 수정 시각이 아니라 **PNG 의 SHA-256**
입니다 (재생성하면 시각은 6장 다 갱신되므로 시각만으로는 판정할 수
없습니다).

### 1단계 — 묶음 재지정만 반영

| 파일 | 결과 |
|---|---|
| `fig1_znion_lr.png` | **그대로** (바이트 동일) |
| `fig2_domains.png` | **바뀜** ← Zn-ion 칸이 5e-5 판으로 |
| `fig3_calb_seeds.png` | **그대로** |
| `fig4_liion_vs_calb.png` | **그대로** |
| `fig5_early_stop.png` | **바뀜** ← Zn-ion 9회가 5e-5 판으로 |
| `fig6_seen_unseen.png` | **그대로** |

지시대로 **2와 5만** 바뀌었습니다. 그림 1 은 두 학습률을 나란히 놓는 것이
목적이라 파일을 직접 지정해 그리므로 묶음과 무관합니다 — 바이트 단위로
동일함을 확인했습니다.

### 2단계 — 지시 §5 의 그림 4 부제 추가

| 파일 | 결과 |
|---|---|
| `fig4_liion_vs_calb.png` | **바뀜** ← 부제 한 줄 추가 |
| 나머지 5장 | **그대로** |

**지시 §2("1·3·4·6 이 바뀌지 않았는지 검증")와 §5(그림 4 부제 추가)가
서로 어긋납니다.** §5 를 따르면 그림 4 는 반드시 바뀝니다. 부제 한 줄
추가가 §5 의 명시적 요구이므로 §5 를 따랐고, 그 변경이 묶음 재지정과
무관함을 보이기 위해 위처럼 단계를 나눠 대조했습니다. **묶음 재지정
자체로는 그림 4 가 바뀌지 않았습니다.**

그림 1 · 3 · 6 은 두 단계 모두 바이트 단위로 동일합니다.

### 그림 2 · 5 에서 무엇이 달라졌는가

| | 재지정 전 | 재지정 후 |
|---|---|---|
| 그림 2 Zn-ion 칸 | lr 5e-4 · 최적 에폭 12 · MAPE 0.9~1.5 대 · 붉은 `[조건 다름]` 제목 | lr 5e-5 · 최적 에폭 10 · MAPE 0.5~1.4 대 · 각주 없음 |
| 그림 5 Zn-ion 9점 | 5e-4/1e-3 판의 종료·최적 에폭 | 5e-5 판의 종료·최적 에폭 |

그림 5 의 **간격 분포는 5가 30회 · 6이 3회 · 7이 3회로 재지정 전과
같습니다.** 상위 6건(간격 6~7)의 명단도 그대로입니다 — Na-ion 세 모델
s2021(간격 7) · Zn-ion MLP s2024 · Na-ion CPTransformer s42 · Li-ion
CPTransformer s2024(간격 6). Zn-ion MLP 는 두 판의 값이 같아 바뀔 것이
없고, CPMLP · CPTransformer 6회는 간격이 전부 5 라 분포가 유지됐습니다.
**앞선 보고 §6 의 상위 5개 표는 정정할 것이 없습니다.**

100에폭 도달 0건도 그대로입니다.

---

## 4. `CONDITIONS.md` 변경 내역

| 절 | 무엇을 |
|---|---|
| **§0 신설** | 정본 기준을 인용문으로 명시 — Zn-ion 은 5e-5 판이 정본, 5e-4 판은 `diagnostic` 으로 보존, 그림 1 이 두 판을 대비. Table 3 값 `0.629 ± 0.080` 과의 대조도 함께 적었습니다 |
| **§0 신설** | 파일명 접미사 규칙이 Zn-ion 만 뒤집힌다는 경고 (§1-4). `group` 이 근거임을 명시 |
| **§1 표** | 그림 1 의 묶음 표시를 뒤집음 (5e-4 → diagnostic · 5e-5 → main) |
| **§1 표** | 그림 2 의 Zn-ion 줄을 `__lr5e-05` · lr 5e-5 로 교체, **"조건 다름" 각주 제거** |
| **§1 표** | 그림 5 의 lr 칸을 "5e-5 (36회 전부)" 로, 비고를 "학습률은 통일, batch_size 는 갈립니다" 로 정정 |
| **§1 표** | 그림 4 의 CALB 줄에 "에폭별 변동이지 반복 간 흩어짐이 아닙니다" 추가 |
| **§2 제목** | "조건이 섞인 그림" → **"읽을 때 주의할 것"** |
| **§2** | 그림 2 항을 "네 칸의 학습률이 같습니다" 로 교체 — 각주를 뗀 경위 포함 |
| **§2** | 그림 5 항의 학습률 분기표를 삭제하고 "학습률은 통일, batch_size 만 갈림" 으로 정정 |
| **§2 신설** | 그림 4 항 — 에폭별 변동과 반복 간 흩어짐이 다른 얘기이고 표본 크기와 관련된 것은 그림 3 이라는 서술 |
| **§2** | 그림 1 항에 "묶음 재지정의 영향을 받지 않는다 · 전후 바이트 동일" 추가 |
| **§4 색 규칙** | "조건 다름 표시" 를 표에서 빼고, 장치는 코드에 남아 있으나 지금은 발동하지 않는다고 주석 |

`experiments/results/` 의 표·JSON 값은 건드리지 않았습니다.

---

## 5. `163` 이 남아 있는 위치 — **정정할 곳이 없습니다**

`docs/` · `findings/` · `verify/` · `train/` · `experiments/` · `manifests/`
· `notebooks/` · `papers/` · 루트 `*.md` 를 훑었습니다. **Li-ion 시험 셀
수를 163 이라고 적은 곳은 한 군데도 없습니다.** 걸린 것은 전부 다른
뜻이거나 맞는 서술입니다.

| 위치 | 문맥 | 판정 |
|---|---|---|
| `docs/RUN.md:491` | `train 515 -> 510 · val 165 -> 165 · test 163 -> 162` | **맞습니다** — 163 은 제외 **전** 값이고 화살표 뒤가 162 |
| `docs/reports/2026-08-04_unattended.md:337` | `test 163 - 1 = 162` | **맞습니다** — 같은 뜻 |
| `docs/reports/2026-08-04_unattended.md:350` | `test 163->162 (합계 843 -> 837)` | **맞습니다** |
| `docs/reports/2026-08-04_unattended.md:401` | `MIX_large 는 여전히 843` — `515 / 165 / 163` | **맞습니다** — `MIX_large`(제외 전)의 값이고, 바로 아래 줄이 `MIX_large_841 = 510/165/162` |
| `docs/reports/2026-08-04_plan.md:92` | `\| MIX_large \| 515 \| 165 \| 163 \| 843 \|` | **맞습니다** — `MIX_large`(제외 전) 표 |
| `verify/check_841.py:43` | `EXPECT_MIX = {"train": 515, "val": 165, "test": 163}` | **맞습니다** — 원본 `MIX_large` 가 안 바뀌었는지 보는 검사의 기대값. 162 로 고치면 검사가 깨집니다 |
| `experiments/results/LABEL_REPORT.md` 4곳 | XJTU 셀의 수명 라벨 163 · `[0.146163, 1.0]` 등 | **무관** — 셀 수가 아닙니다 |
| `LOCK.md:70` | 해시 문자열 안의 `163` | **무관** |

즉 **163 은 `MIX_large`(843셀)의 시험 분할이고 162 는 라벨 미배포 1셀을
뺀 `MIX_large_841`(837셀)의 시험 분할**입니다. 두 값이 가리키는 대상이
달라 저장소 안에서는 충돌이 없습니다. 앞선 지시서가 163 을 최종 셀 수로
적은 것이 유일한 어긋남이었고, 그림은 이미 162 로 되어 있습니다.

지시대로 **수정은 하지 않았습니다.**

---

## 6. 판단이 필요한 것

### 6-1. Zn-ion JSON 파일명을 정본에 맞출지

지금 `CPMLP_Zn-ion_s2021.json` 은 `diagnostic`(5e-4) 이고
`CPMLP_Zn-ion_s2021__lr5e-05.json` 이 `main` 입니다. **Zn-ion 만 접미사
규칙이 뒤집혀 있습니다.** 그림은 `group` 으로 고르므로 문제가 없지만,
사람이 파일 목록만 보면 반대로 읽습니다.

맞추려면 9쌍의 이름을 바꿔야 합니다 — 정본이 접미사 없는 이름을 갖고
문서값 판이 `__lr_doc`(또는 `__lr5e-04`) 을 갖는 형태입니다. 이번 지시가
"묶음 표시만" 이어서 하지 않았습니다. **할지 확정해 주십시오.**
`train/curves.py` 의 `SOURCES` 태그 한 줄과 재지정 스크립트만 바꾸면
됩니다.

### 6-2. 앞선 보고서 `2026-08-05_figures.md` 를 어떻게 할지

그 보고서 §4 · §7-1 이 재지정 전 상태를 서술하고 있어 지금과 어긋납니다.
구체적으로

- §4 요약표: "그림 2 의 Zn-ion 칸만 5e-4" · "그림 5 는 조건이 섞임"
- §7-1: "지시서를 따랐습니다 — main = `20260804-154322_*`"

지시가 **보고서 수정 금지**여서 손대지 않았습니다. 이 보고서가 정정본
이라는 것만 이 문서 머리에 적어 뒀습니다. §6 · §5 의 관찰(간격 분포 ·
상위 5개 · 그림 1 의 Train Loss 값)은 **재지정 후에도 그대로 유효**합니다
(§3 에서 확인). **앞 보고서에 정정 표시를 달지, 그대로 둘지 정해
주십시오.**

### 6-3. `runs/2026-08-04/README.md` 와 `docs/PLAN.md` §7-3

README 는 이번 정정과 같은 내용을 이미 적고 있어 고칠 것이 없습니다.
`PLAN.md` §7-3 과 `findings` TRN-012 가 이 갈린 지점을 다루는데, 이번에
읽기만 하고 손대지 않았습니다. 갱신이 필요한지는 판단 밖입니다.

### 6-4. 그림 3 을 반복 간 흩어짐의 근거로 쓸 것인지

그림 4 부제가 "반복 간 흩어짐은 그림 3 을 보십시오" 로 그림 3 을
가리키게 됐습니다. 그림 3 은 **CALB 3 seed 만** 보여 주므로 Li-ion 쪽
흩어짐은 어느 그림에도 없습니다. 표본 크기 대비를 그림으로 세우려면
**Li-ion 3 seed 와 CALB 3 seed 를 같은 축에 놓는 그림이 하나 더**
필요합니다. 지시 범위 밖이라 만들지 않았습니다.

---

## 7. 하지 않은 것

- `git commit` · `tag` · `push` — 하지 않았습니다 (`git add` 만).
- 재학습 · 재파싱 — 하지 않았습니다. `group` 필드만 바꿨습니다.
- `upstream/` 수정 — 하지 않았습니다.
- 그림 1 · 3 · 6 재설계 — 하지 않았습니다 (바이트 단위로 동일).
- `experiments/results/` 의 표·JSON **값** 수정 — 하지 않았습니다 (58/58 대조).
- 보고서(`docs/*.md`) 수정 — 하지 않았습니다. 이 파일만 새로 썼고 163 은
  목록만 만들었습니다.
- torch 재설치 — 하지 않았습니다.
- 결과 해석 · `verdict` 작성 — 하지 않았습니다.
